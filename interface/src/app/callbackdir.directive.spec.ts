import { CallbackdirDirective } from './callbackdir.directive';

describe('CallbackdirDirective', () => {
  it('should create an instance', () => {
    const directive = new CallbackdirDirective();
    expect(directive).toBeTruthy();
  });
});
